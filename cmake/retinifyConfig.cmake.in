@PACKAGE_INIT@

if(DEFINED RETINIFY_CONFIG_LOADED)

else()
set(RETINIFY_CONFIG_LOADED TRUE)
include("${CMAKE_CURRENT_LIST_DIR}/retinifyTargets.cmake")
message(STATUS "Found retinify v@PROJECT_VERSION@")

endif()